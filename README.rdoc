1. Run the server with "rails s" command
2. Migrate data typing "rake db:migrate"
3. Sign Up user with "curl -X POST -H "Content-Type: application/json" -d '{"user": {"email": "ororo@mail.com", "password": "abagalamaga"}}' http://localhost:3000/users" command or throw Postman
4. Try to Sign Up with same email and you should get error message says that email in use

